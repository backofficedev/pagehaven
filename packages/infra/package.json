{
  "name": "@pagehaven/infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./resources": {
      "default": "./src/resources.ts"
    },
    "./helpers": {
      "default": "./src/helpers.ts"
    },
    "./constants": {
      "default": "./src/constants.ts"
    }
  },
  "scripts": {
    "check-types": "tsc -b",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@pagehaven/config": "workspace:*",
    "alchemy": "catalog:",
    "vitest": "catalog:"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
